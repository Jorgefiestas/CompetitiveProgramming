#include <bits/stdc++.h>
using namespace std;
long long n;
int t, s;


long long solve() {
	cin >> n >> s;

	for (int i  = 0; i <= n; i++) {
		f
	}

	return ans - n;
}

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin >> t;
	while (t--) {
		cout << solve() << '\n';
	}

	return 0;
}
