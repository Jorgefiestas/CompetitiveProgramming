#include <bits/stdc++.h>
using namespace std;
const int N = 1005;
int n, l[N];

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> l[i];
	}

	

	return 0;
}
