#include <bits/stdc++.h>
using namespace std;
int t;
string n;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin >> t;
	while (t--) {
		cin >> n;
		cout << n.size() << '\n';
	}

	return 0;
}
