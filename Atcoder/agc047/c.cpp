#include <bits/stdc++.h>
using namespace std;
const int P = 2e5 + 3;
const int N = 2e5 + 100;
int n, a[N];

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin >> n;

	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}

	return 0;
}
